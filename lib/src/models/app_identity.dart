/// The single source of the app's user-facing name: the window title, the
/// Flutter title bar, and any future chrome all read this constant so the
/// name can never drift between surfaces.
const appDisplayName = 'Type Mate';
