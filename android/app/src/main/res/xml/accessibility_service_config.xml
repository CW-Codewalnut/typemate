<?xml version="1.0" encoding="utf-8"?>
<!-- Watches input focus so the floating mic can appear exactly when an
     editable field is focused, and inserts the transcript into that
     field. No content is read beyond the focused field's text (needed to
     append rather than overwrite); nothing leaves the device. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewFocused|typeViewClicked|typeWindowStateChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
